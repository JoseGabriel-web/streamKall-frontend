import React, { FC } from 'react'

interface props {
    display: boolean
}

const Participants:FC = () => {
    return (
        <div>
            
        </div>
    )
}

export default Participants
