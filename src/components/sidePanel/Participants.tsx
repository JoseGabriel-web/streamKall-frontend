import React, { FC } from 'react'

interface props {
    display: boolean
}

const Participants:FC = () => {
    return (
        <div>
            Participants
        </div>
    )
}

export default Participants
